{% extends 'base.html.twig' %}

{% block body %}


    <div class="authentification_background">
        <section class="container d-flex justify-content-center">
            <div class="form-group">
                <div class="form-class" style="margin-top:100px; margin-bottom:100px;">
                    <div class="m-4 m-4">
                        <p class="pt-3 text-center form-title"><strong>Modifier l'illustration</strong></p>
                            <img src="/images/avatars/{{media.url}}">
                            {{ form_start(editMediaForm) }}
                            <div>Nom du Trick :</div>
                            <div>{{ form_row(editMediaForm.media)}}</div><br>
                            {{ form_end(editMediaForm) }}
                    </div>
                </div>
            </div>
        </section>
    </div>

{% endblock %}